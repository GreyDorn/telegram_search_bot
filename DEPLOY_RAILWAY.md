# 🚂 Развертывание на Railway.app

Railway.app - современная платформа для развертывания приложений. Простая настройка, бесплатный план на старте.

## ✅ Преимущества Railway:

- 🆓 Бесплатный план (500 часов в месяц)
- ⚡ Быстрое развертывание (5 минут)
- 🔄 Автоматические обновления из GitHub
- 💾 Хранение переменных окружения
- 📊 Логи и мониторинг

---

## 🚀 Пошаговая инструкция

### Шаг 1: Регистрация на Railway

1. Открой https://railway.app
2. Нажми "Start a New Project"
3. Войди через GitHub

### Шаг 2: Подготовка проекта

#### 2.1. Загрузи проект на GitHub

Открой PowerShell в папке проекта:

```bash
cd C:\Users\user\projects\telegram_search_bot

# Инициализация Git (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit"

# Создай репозиторий на GitHub и подключи его
git remote add origin https://github.com/ТвойUsername/telegram_search_bot.git
git branch -M main
git push -u origin main
```

### Шаг 3: Создание проекта на Railway

1. На Railway нажми "New Project"
2. Выбери "Deploy from GitHub repo"
3. Выбери свой репозиторий `telegram_search_bot`
4. Railway автоматически определит Python проект

### Шаг 4: Настройка переменных окружения

1. Открой свой проект на Railway
2. Перейди в "Variables"
3. Добавь переменные:

```env
API_ID=12345678
API_HASH=abcdef1234567890abcdef1234567890
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
PHONE=+79991234567
CHANNELS=@channel1,@channel2,@channel3
SEARCH_LIMIT=1000
```

### Шаг 5: Настройка команды запуска

Railway должен автоматически запустить бота, но можно настроить вручную:

1. В проекте → Settings → Deploy
2. Start Command: `python bot_telethon.py`

### Шаг 6: Авторизация в Telegram (ВАЖНО!)

⚠️ **Проблема:** Railway не позволяет вводить код авторизации интерактивно.

**Решение:** Авторизуйся локально, затем загрузи session файл:

#### Вариант А: Авторизация локально

1. Запусти бота локально (на своем ПК):
```bash
python bot_telethon.py
```

2. Введи код авторизации

3. Появится файл `bot_session.session`

4. Загрузи этот файл в Git:
```bash
# ВРЕМЕННО удали session из .gitignore
# Открой .gitignore и закомментируй строки:
# *.session
# *.session-journal

git add bot_session.session
git commit -m "Add session file"
git push
```

5. Railway автоматически подхватит изменения

6. **ВАЖНО:** Верни session обратно в .gitignore!

#### Вариант Б: Создать UserBot без интерактивной авторизации

Создам специальную версию бота для Railway (см. ниже).

### Шаг 7: Деплой

1. Railway автоматически запустит деплой
2. Следи за логами: вкладка "Deployments"
3. Если всё ОК, увидишь: `✅ Бот запущен и готов к работе!`

---

## 🔧 Специальный файл для Railway

Создай файл `railway.json` в проекте:

```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python bot_telethon.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

---

## 📊 Мониторинг

### Проверка логов:

1. Открой проект на Railway
2. Вкладка "Deployments"
3. Кликни на активный деплой
4. Смотри логи в реальном времени

### Проверка работы бота:

1. Открой бота в Telegram
2. Отправь `/start`
3. Попробуй поиск

---

## ⚠️ Важные моменты

### Бесплатный план Railway:

- 500 часов в месяц
- 512 MB RAM
- 1 GB диск
- Достаточно для одного бота

### Оплата после лимита:

- $5/месяц за базовый план
- Pay-as-you-go модель

### Ограничения:

- ❌ Нет интерактивного ввода (авторизация - проблема)
- ✅ Автоматические перезапуски
- ✅ Постоянное хранилище

---

## 🐛 Проблемы и решения

### "Session file not found"

✅ **Решение:** Загрузи `bot_session.session` в Git (см. Шаг 6)

### "API_ID не настроен"

✅ **Решение:** Проверь переменные окружения в Railway

### Бот не отвечает

✅ **Решение:** 
1. Проверь логи на Railway
2. Убедись, что деплой успешный (зелёная галка)
3. Проверь переменные окружения

### "Cannot import telethon"

✅ **Решение:** Убедись, что `requirements_telethon.txt` в Git

---

## 💡 Рекомендации

1. **Используй .env локально**, но на Railway - переменные через интерфейс
2. **Session файл** - авторизуйся локально, потом загрузи
3. **Логи** - регулярно проверяй на ошибки
4. **Бэкап** - сохрани `bot_session.session` в надежном месте

---

## 🔄 Обновление бота

Чтобы обновить бота:

```bash
# Внеси изменения в код
# Закоммить и запушь
git add .
git commit -m "Update bot"
git push
```

Railway автоматически развернет новую версию! 🚀

---

## 📞 Альтернатива

Если Railway не подходит (проблема с авторизацией):
- Используй VPS (см. `DEPLOY_VPS.md`)
- Используй Render.com (см. `DEPLOY_RENDER.md`)

---

Готово! Бот будет работать 24/7 на Railway! 🎉
