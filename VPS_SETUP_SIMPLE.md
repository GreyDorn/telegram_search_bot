# 🚀 Простая инструкция по настройке VPS (для новичков)

Пошаговая инструкция на понятном языке. Просто копируй команды и выполняй.

---

## 📋 Что нужно подготовить

Перед началом убедись, что у тебя есть:

1. ✅ Аккаунт на GitHub
2. ✅ Данные для бота:
   - `API_ID` и `API_HASH` (получи на https://my.telegram.org)
   - `BOT_TOKEN` (получи у @BotFather)
   - Твой номер телефона в формате `+79991234567`
   - Список каналов (например: `@channel1,@channel2`)

---

## Шаг 1: Загрузка проекта на GitHub (делаем на твоём ПК)

### 1.1. Открой PowerShell в папке проекта

```powershell
cd C:\Users\user\projects\telegram_search_bot
```

### 1.2. Загрузи проект на GitHub

**Если репозиторий уже создан:**

```powershell
git add .
git commit -m "Подготовка к деплою на VPS"
git push
```

**Если репозиторий ещё не создан:**

1. Открой https://github.com
2. Нажми "New repository"
3. Назови: `telegram_search_bot`
4. Нажми "Create repository"
5. Выполни команды:

```powershell
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ТВОЙ_USERNAME/telegram_search_bot.git
git push -u origin main
```

**✅ Проект теперь на GitHub!**

---

## Шаг 2: Аренда VPS

### Рекомендую DigitalOcean (простой и надёжный)

**Цена:** $6/месяц  
**Время настройки:** 5 минут

### 2.1. Регистрация

1. Открой https://digitalocean.com
2. Зарегистрируйся (нужна банковская карта)
3. Подтверди email

### 2.2. Создание сервера (Droplet)

1. Нажми **"Create"** → **"Droplets"**
2. Выбери настройки:
   - **Образ:** Ubuntu 22.04 LTS
   - **План:** Basic ($6/мес)
   - **CPU:** Regular (1 GB / 1 vCPU)
   - **Регион:** Выбери ближайший к тебе
3. В разделе **"Authentication"**:
   - Выбери **"Password"** (проще для новичка)
   - Придумай надёжный пароль и запиши его!
4. Нажми **"Create Droplet"**

### 2.3. Получение IP адреса

Через 1-2 минуты сервер создастся. Ты увидишь IP адрес типа: `123.456.789.123`

**✅ Запиши этот IP - он понадобится!**

---

## Шаг 3: Подключение к серверу через SSH

### 3.1. Открой PowerShell на своём ПК

### 3.2. Подключись к серверу

```powershell
ssh root@123.456.789.123
```

**Замени `123.456.789.123` на свой IP!**

### 3.3. Введи пароль

- При вводе пароля **символы не отображаются** - это нормально!
- Просто вводи и нажимай Enter

**Если спросит "Are you sure you want to continue connecting?" - пиши `yes` и Enter**

**✅ Если увидел приглашение `root@ubuntu:~#` - ты подключился!**

---

## Шаг 4: Настройка сервера (просто копируй команды)

Сейчас будем настраивать сервер. Просто копируй команды одну за другой.

### 4.1. Обновление системы

```bash
apt update && apt upgrade -y
```

⏳ Подожди 1-3 минуты

### 4.2. Установка Python, pip и Git

```bash
apt install python3 python3-pip git nano -y
```

⏳ Подожди 1-2 минуты

### 4.3. Проверка версии Python

```bash
python3 --version
```

Должно быть **Python 3.10** или выше

**✅ Сервер готов!**

---

## Шаг 5: Загрузка проекта на сервер

### 5.1. Клонирование с GitHub

```bash
cd ~
git clone https://github.com/ТВОЙ_USERNAME/telegram_search_bot.git
cd telegram_search_bot
```

**Замени `ТВОЙ_USERNAME` на свой логин GitHub!**

### 5.2. Установка зависимостей для бота

```bash
pip3 install -r requirements_telethon.txt
```

⏳ Подожди 1-2 минуты

**✅ Проект загружен!**

---

## Шаг 6: Настройка .env файла

### 6.1. Создание .env файла

```bash
nano .env
```

Откроется текстовый редактор.

### 6.2. Вставь свои данные

Скопируй и вставь (правой кнопкой мыши):

```env
API_ID=12345678
API_HASH=abcdef1234567890abcdef1234567890
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
PHONE=+79991234567
CHANNELS=@channel1,@channel2
SEARCH_LIMIT=1000
```

**Замени на свои данные:**
- `API_ID` - твой ID с https://my.telegram.org
- `API_HASH` - твой Hash с https://my.telegram.org
- `BOT_TOKEN` - токен от @BotFather
- `PHONE` - твой номер в формате `+79991234567`
- `CHANNELS` - твои каналы через запятую

### 6.3. Сохранение файла

1. Нажми `Ctrl + X`
2. Нажми `Y` (согласие на сохранение)
3. Нажми `Enter`

**✅ Настройки сохранены!**

---

## Шаг 7: Первый запуск и авторизация

### 7.1. Запуск бота

```bash
python3 bot_telethon.py
```

### 7.2. Авторизация

Бот попросит ввести код из Telegram:

1. Открой Telegram на телефоне
2. Получишь сообщение с кодом
3. Введи код (без пробелов и дефисов)
4. Нажми Enter

**Если бот запустился и пишет "Бот запущен" - всё работает!**

### 7.3. Остановка бота

Нажми `Ctrl + C`

**✅ Авторизация прошла успешно! Создался файл `bot_session.session`**

---

## Шаг 8: Настройка автозапуска (чтобы бот работал всегда)

Сейчас настроим, чтобы бот:
- Запускался автоматически
- Работал в фоне
- Перезапускался после сбоев

### 8.1. Создание службы systemd

```bash
nano /etc/systemd/system/telegram-bot.service
```

### 8.2. Вставь эту конфигурацию

```ini
[Unit]
Description=Telegram Search Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/telegram_search_bot
ExecStart=/usr/bin/python3 /root/telegram_search_bot/bot_telethon.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### 8.3. Сохрани файл

1. `Ctrl + X`
2. `Y`
3. `Enter`

### 8.4. Запуск службы

Выполни команды по очереди:

```bash
# Перезагрузка конфигурации
systemctl daemon-reload

# Запуск бота
systemctl start telegram-bot

# Автозапуск при включении сервера
systemctl enable telegram-bot

# Проверка статуса
systemctl status telegram-bot
```

**Должно быть:** `Active: active (running)` - зелёная надпись

**Чтобы выйти из статуса - нажми `q`**

**✅ Бот работает 24/7!**

---

## 🎉 Готово! Теперь проверь бота

1. Открой Telegram
2. Найди своего бота
3. Отправь `/start`
4. Отправь ключевое слово для поиска

**Бот должен ответить и найти результаты!**

---

## 📊 Полезные команды для управления

### Посмотреть логи бота (последние 50 строк)

```bash
journalctl -u telegram-bot -n 50
```

### Посмотреть логи в реальном времени

```bash
journalctl -u telegram-bot -f
```

**Чтобы выйти - нажми `Ctrl + C`**

### Перезапустить бота

```bash
systemctl restart telegram-bot
```

### Остановить бота

```bash
systemctl stop telegram-bot
```

### Запустить бота

```bash
systemctl start telegram-bot
```

### Проверить статус

```bash
systemctl status telegram-bot
```

---

## 🔄 Как обновить бота в будущем

Если ты изменил код и хочешь обновить бота на сервере:

### Вариант 1: Через Git (рекомендуется)

**На своём ПК:**
```powershell
cd C:\Users\user\projects\telegram_search_bot
git add .
git commit -m "Обновление"
git push
```

**На сервере:**
```bash
cd ~/telegram_search_bot
git pull
systemctl restart telegram-bot
```

### Вариант 2: Прямое копирование файла

**На своём ПК (в PowerShell):**
```powershell
scp bot_telethon.py root@123.456.789.123:/root/telegram_search_bot/
```

**На сервере:**
```bash
systemctl restart telegram-bot
```

---

## 🐛 Если что-то пошло не так

### Бот не запускается

```bash
# Проверь логи - там будет описание ошибки
journalctl -u telegram-bot -n 100
```

### "Module not found" ошибка

```bash
cd ~/telegram_search_bot
pip3 install -r requirements_telethon.txt --force-reinstall
systemctl restart telegram-bot
```

### Бот не отвечает

```bash
# Проверь, работает ли бот
systemctl status telegram-bot

# Если не работает - запусти
systemctl start telegram-bot
```

### Забыл IP адрес сервера

Зайди на https://digitalocean.com → Droplets → найдёшь IP

---

## 🔒 Безопасность (необязательно, но рекомендуется)

### Настрой файрвол

```bash
apt install ufw -y
ufw allow ssh
ufw allow 22
ufw enable
```

При вопросе нажми `y` и Enter

---

## 💡 Что дальше?

✅ **Бот работает 24/7**  
✅ **Автоматический перезапуск**  
✅ **Логи сохраняются**  

**Теперь можешь:**
- Отключить свой компьютер - бот продолжит работать
- Искать по каналам из любой точки мира
- Добавлять новые каналы через обновление `.env`

---

## 📞 Где получить данные

### API_ID и API_HASH:
1. https://my.telegram.org
2. API development tools
3. Create new application
4. Скопируй `api_id` и `api_hash`

### BOT_TOKEN:
1. Найди @BotFather в Telegram
2. Отправь `/newbot`
3. Следуй инструкциям
4. Скопируй токен

---

## 💰 Стоимость

**DigitalOcean:** $6/мес  
**Первый месяц:** может быть промокод на $200 для новых пользователей

**Альтернативы:**
- **Vultr** - от $2.5/мес
- **Timeweb (РФ)** - от 150₽/мес

---

**Готово! Теперь твой бот работает в облаке! 🚀**

Если возникнут вопросы - проверь логи командой:
```bash
journalctl -u telegram-bot -n 100
```
